package service;

public class UserService {

    public UserService(long id, String imageUrl, String name, String description, String location, int followingCount, int followersCount) {
        super(id, imageUrl, name, description, location, followingCount, followersCount);
    }
}
